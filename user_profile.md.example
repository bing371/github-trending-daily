---
# 这份文件是 AI 讲解 GitHub 项目时的「听众画像」。
# 它会被 server.py 读取，填到 system prompt 的「我的个人身份」一节。
#
# 想换人设？只改这一份文件就行。
# 把下面「---」分隔线之后的几行换成你自己的实际情况（职业、技术栈、关注方向、痛点），
# 改完保存，重启 server.py 即可生效。
# 文件用 UTF-8 编码保存，避免中文乱码。
# （分隔线以上的注释会被自动忽略，server.py 只读取分隔线以下的内容。）
#
# ⚠️ 这个文件叫 .example —— 它是模板，不含个人信息，可以安全 commit。
#    真正生效的是 user_profile.md（被 .gitignore 排除，install.ps1 会帮你生成）。
---

- 职业：（比如：前端工程师 / 室内设计师 / 独立开发者 / 产品经理 …）
- 技术栈：（比如：TypeScript、React、Python 基础；或「会用 Figma + ComfyUI」）
- 当前关注方向：（比如：AI 内容生成、效率工具、工作流自动化）
- 痛点：（比如：想加速产出；想试着自己做点小工具；想找现成工具减少重复劳动）